# Claude Code - Development Guidelines

このドキュメントはClaude Codeが開発時に従うべきガイドラインを定義しています。

## 原則

1. **品質第一**: すべての変更はテストとドキュメンテーションを含む
2. **段階的改善**: 小さな機能を積み重ねる
3. **ユーザー中心**: ユーザー体験を最優先
4. **自動化**: 手動作業を最小化

## 変更手順

### 1. 計画フェーズ
- タスクを分解して管理可能にする
- 既存コードを分析して影響範囲を確認
- 実装方法を決定

### 2. 実装フェーズ
- コードを実装
- テストを作成
- エラーハンドリングを追加

### 3. テストフェーズ
- ユニットテストを実行
- 統合テストを実行
- 手動検証を実施

### 4. ドキュメントフェーズ
- CHANGELOG.mdを更新
- コードコメントを追加（必要に応じて）
- READMEを更新（必要に応じて）

### 5. コミット・デプロイフェーズ
- `git add`でファイルを追加
- `git commit`でコミット
- `git push`でプッシュ
- GitHub Actionsが自動デプロイを実行

## コード品質標準

### Dart/Flutter
- Null safety対応
- Provider/Riverpodを使用した状態管理
- Widgetの責任を明確に分離
- Hiveを使用したローカルストレージ

### テスト
- 新機能には対応するテストを作成
- テストカバレッジ > 70%を目指す
- 統合テストで実際の動作を確認

### ドキュメント
- 公開APIはコメント必須
- 複雑なロジックは説明を追加
- CHANGELOG.mdは常に最新に維持

## GitHub Actions ワークフロー

```
push → lint/format → build → test → deploy
```

1. **lint/format**: コードスタイルのチェック
2. **build**: プロジェクトのビルド
3. **test**: テストスイートの実行
4. **deploy**: GitHub Pagesへのデプロイ

## セキュリティ

- 機密情報は`.env.example`として例示
- GitHub Secretsを使用した安全な秘密管理
- 依存関係の定期的な更新

## パフォーマンス

- Widgetの不要なリビルドを防止
- 大きなリストはLazyLoading実装
- 画像の最適化と圧縮

## ブランチ戦略

- `main`: 本番対応コード
- `gh-pages`: Web デプロイ用（自動生成）
- Feature branches: 機能ごとに作成（必要に応じて）

## Commit メッセージのルール

すべてのコミットは以下の形式に従う：

```
<type>: <subject>

<body>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### Types
- `Feat`: 新機能
- `Fix`: バグ修正
- `Docs`: ドキュメント
- `Style`: フォーマット変更
- `Refactor`: コード整理
- `Test`: テスト追加
- `Chore`: ビルド・依存関係

## CHANGELOG.md 更新ルール

すべての変更は以下のカテゴリで CHANGELOG.md に記録：

- **Added**: 新機能
- **Fixed**: バグ修正
- **Changed**: 既存機能の変更
- **Deprecated**: 非推奨機能
- **Removed**: 削除された機能
- **Security**: セキュリティ関連

記入例：
```markdown
### Added
- FloatingActionButton functionality for creating todos
- Todo list display with checkbox completion
- Persistent storage using Hive database

### Fixed
- Missing web/index.html for Flutter Web builds
- GitHub Actions permissions for gh-pages deployment

### Changed
- HomeScreen from StatelessWidget to StatefulWidget
- Improved UI with Consumer widget for state management
```

## レビューとマージ

1. すべてのテストがパスすること
2. CHANGELOG.mdが更新されていること
3. コードスタイルが統一されていること
4. ドキュメントが最新であること

---

**最終更新**: 2025-11-19
